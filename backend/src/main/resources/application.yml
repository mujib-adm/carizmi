server:
  port: 8080
  servlet:
    context-path: /api

spring:
#  main:
#    banner-mode: off
#    lazy-initialization: true
#    web-application-type: servlet
  datasource:
    url: ${DB_URL}
    username: ${DB_USER}
    password: ${DB_PASS}
    driver-class-name: com.mysql.cj.jdbc.Driver
  data:
    redis:
      host: ${SPRING_DATA_REDIS_HOST:redis}
      port: ${SPRING_DATA_REDIS_PORT:6379}
  jpa:
    hibernate:
      ddl-auto: update
      naming:
        physical-strategy: org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl
      #none
    #generate-ddl: false
    show-sql: false
    properties:
      hibernate:
        default_catalog: sofumar
        default_schema: sofumar
#        hbm2ddl:
#          auto: none
        format_sql: true
        jdbc:
          time_zone: UTC
  flyway:
    enabled: false
    #    locations: classpath:db/migration
  liquibase:
    enabled: false
#  redis:
#    host: ${SPRING_REDIS_HOST:redis}
#    port: ${SPRING_REDIS_PORT:6379}
#  sql:
#    init:
#      platform: mysql
#  data:
#    jpa:
#      repositories:
#        bootstrap-mode: deferred

#jakarta:
#  persistence:
#    jdbc:
#      url: jdbc:mysql://db:3306/sofumar?useSSL=false

jwt:
  issuer: sofumar.portal
  secret: ${JWT_SECRET}
  expirationMinutes: 5
  refreshTokenExpirationMinutes: 15

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics

migration:
  resources:
    dir: backend/src/main/resources

app:
  cors:
    allowed-origins: ${APP_CORS_ALLOWED_ORIGINS:http://localhost:8081,http://localhost:5173}

#  temporarily logging spring for debugging purposes
#logging:
#  level:
#    liquibase: DEBUG
#    org.liquibase: DEBUG
#    org.liquibase.ext.hibernate: DEBUG
#    root: DEBUG
#    org.hibernate: DEBUG
#    org.springframework.data: DEBUG
#    org.springframework.orm.jpa: DEBUG
#    org.springframework.transaction: DEBUG
#    org.springframework: DEBUG
#    org.springframework.security: DEBUG
#    org.springframework.web: DEBUG
#    org.springframework.http.converter.json: DEBUG
#    com.fasterxml.jackson: DEBUG